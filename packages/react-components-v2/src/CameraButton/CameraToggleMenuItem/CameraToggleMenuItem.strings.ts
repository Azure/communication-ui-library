// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.

/**
 * Locale strings of {@link CameraToggleMenuItem}.
 *
 * @public
 */
export interface CameraToggleMenuItemStrings {
  /** Label for the CameraToggleMenuItem when the camera is on. */
  cameraOnLabel: string;
  /** Label for the CameraToggleMenuItem when the camera is off. */
  cameraOffLabel: string;
}
