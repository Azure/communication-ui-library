// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.

/**
 * Locale strings of {@link CameraButtonTooltip}.
 *
 * @public
 */
export interface CameraButtonTooltipStrings {
  /** Content for the tooltip when the camera is on. */
  cameraOnContent: string;
  /** Content for the tooltip when the camera is off. */
  cameraOffContent: string;
  /** Content for the tooltip when the camera is disabled. */
  cameraButtonDisabledContent: string;
  /** Content for the tooltip when the camera is disabled due to video loading */
  cameraButtonVideoLoadingContent: string;
}
