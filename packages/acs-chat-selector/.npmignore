# Blacklist everything by default so new files aren't accidently included:
*

# Whitelist dist folder, this is the folder that should be packed and published:
!dist/**/*

# Exceptions in the dist folder that should still be blacklisted:
dist/dist-esm/mocks/
**/*.test.*
**/*.spec.*
