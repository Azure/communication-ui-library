import { RealTimeText } from '@internal/react-components';
import { Canvas, Meta, ArgTypes } from '@storybook/blocks';
import * as RTTStories from './index.stories';
import RTTExampleText from '!!raw-loader!./snippets/ExampleRTT.snippet.tsx';

<Meta of={RTTStories} />

# Real Time Text

A component for displaying a Real time text message with user icon, displayName and captions text.

<Canvas of={RTTStories.ExampleRTTExampleDocsOnly} source={{ code: RTTExampleText }} />

## Props

<ArgTypes of={RealTimeText} />
