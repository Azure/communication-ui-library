import { Canvas, Meta } from '@storybook/blocks';
import * as ComplianceBannerStories from './index.stories';
import ComplianceBannerExampleText from '!!raw-loader!./snippets/ComplianceBanner.snippet.tsx';

<Meta of={ComplianceBannerStories} />

# Compliance Banner

A component that displays banners to notify the user when call recording and transcription is enabled or disabled in a call.

This component implements a state machine that tracks the changes to call recording and transcription state and shows the corresponding message.

<Canvas of={ComplianceBannerStories.ComplianceBanner} source={{ code: ComplianceBannerExampleText }} />
