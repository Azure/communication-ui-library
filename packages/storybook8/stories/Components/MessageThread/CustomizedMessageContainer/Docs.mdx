import { Canvas, Meta } from '@storybook/blocks';
import * as MessageThreadWithCustomMessageContainerStories from './index.stories';

import MessageThreadWithCustomMessageContainerExample from '!!raw-loader!./snippets/CustomMessageContainer.snippet.tsx';

<Meta of={MessageThreadWithCustomMessageContainerStories} />

## Messages with Customized Message Container

The example below shows how to render a `custom` message container with `styles.chatMessageContainer` or
`styles.systemMessageContainer` in `MessageThread`

Note: In the code example, all `%` characters were replaced by their unicode value `\u0025` due to URI
malformed issue when loading the storybook snippets

## Example

<Canvas
  of={MessageThreadWithCustomMessageContainerStories.CustomMessageContainerDocsOnly}
  source={{ code: MessageThreadWithCustomMessageContainerExample }}
  layout="padded"
/>
