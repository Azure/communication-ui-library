import { Canvas, Description, Meta, Source, ArgTypes } from '@storybook/blocks';
import { RaiseHandButton } from '@azure/communication-react';
import * as RaiseHandStories from './index.stories';
import CustomButtonExampleText from '!!raw-loader!./snippets/Custom.snippet.tsx';
import DefaultButtonExampleText from '!!raw-loader!./snippets/Default.snippet.tsx';
import ButtonWithLabelExampleText from '!!raw-loader!./snippets/WithLabel.snippet.tsx';

<Meta of={RaiseHandStories} />

# Raise Hand Button

A button to toggle the user's raised hand on and off. For use with the [Control Bar](./?path=/docs/components-controlbar-control-bar--docs).

Note that for accessibility purposes, this button can be focused on even when disabled. This behaviour can be changed by setting the button prop `allowDisabledFocus` to `false`.

## Importing

```tsx
import { RaiseHandButton } from '@azure/communication-react';
```

## Example

The default `RaiseHandButton` component shows a RaiseHand icon with no label. The following example displays `RaiseHandButton` with raised and lowered hand.

<Canvas of={RaiseHandStories.RaiseHandButtonExampleDocsOnly} source={{ code: DefaultButtonExampleText }} />

## RaiseHand with default label

You can display the button label which, by default, will show below the icon as `Raise Hand` or `Lower Hand`.

<Canvas of={RaiseHandStories.RaiseHandButtonWithLabelExampleDocsOnly} source={{ code: ButtonWithLabelExampleText }} />

## Custom RaiseHandButton Styles

You can change the styles of the `RaiseHandButton` as you would customized any Button (styles, primary, onRenderIcon, onRenderText, etc... ).

Note: When overriding a render, like using `onRenderIcon` or `onRenderText`, do not forget to add a unique key to each element to avoid warning for children in a list.

<Canvas of={RaiseHandStories.CustomRaiseHandButtonExampleDocsOnly} source={{ code: CustomButtonExampleText }} />

## Props

`RaiseHandButton` features all props a [FluentUI Button](https://developer.microsoft.com/fluentui#/controls/web/button) offers, with the following additional properties.

<ArgTypes of={RaiseHandButton} />
