import { Meta, Source } from '@storybook/blocks';
import * as IncomingCallModalStories from './index.stories';
import exampleIncomingCallModalText from '!!raw-loader!./snippets/exampleIncomingCallModal.snippet.tsx';

<Meta of={IncomingCallModalStories} />

# Incoming Call Alerts

The Incoming Call Alert Components alert about an incoming call. They can render a local video preview, custom
avatar image, caller name and incoming call alert text.

## Incoming Call Modal

<Source code={exampleIncomingCallModalText} />
