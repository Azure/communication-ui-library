import { Meta } from '@storybook/addon-docs/blocks';
import { Canvas, Description, Heading, Props, Source, Title } from '@storybook/addon-docs/blocks';

<Meta title="Stateful Client/Reference" />

# Stateful Client Schema

The stateful client for calling and chat have organized data in a particular schema in order to store communications information during a session. Understanding where this
data lives will allow you to develop selectors for your custom components that need to be powered by Azure Communication Services.

## CallClientState Schema

## ChatClientState Schema
