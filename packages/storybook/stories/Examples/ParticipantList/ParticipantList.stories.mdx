import { Title, Description, Source, Canvas } from '@storybook/addon-docs/blocks';
import { BasicParticipantListExample } from './examples/BasicParticipantList.example';
import BasicParticipantListExampleText from '!!raw-loader!./examples/BasicParticipantList.example.tsx';
import { InteractiveParticipantListExample } from './examples/InteractiveParticipantList.example';
import InteractiveParticipantListExampleText from '!!raw-loader!./examples/InteractiveParticipantList.example.tsx';
import { Meta } from '@storybook/addon-docs/blocks';
import { EXAMPLES_FOLDER_PREFIX } from '../../constants';

<Meta title={`${EXAMPLES_FOLDER_PREFIX}/Participant List`} />

# Participant List

## Basic example

To build a list of [ParticipantItem](./?path=/docs/ui-components-participantitem--participant-item) components, we recommend
using the Fluent UI [Stack](https://developer.microsoft.com/en-us/fluentui#/controls/web/stack) as a container like shown in the
code below.

<Source code={BasicParticipantListExampleText} />

<Canvas withSource="none">
  <BasicParticipantListExample />
</Canvas>

## Interactive example

The participant item is designed with a context menu style affordance which allows you to sub menu items for interacting
with this participant. For example, let us add menu items and icons to the participants using `menuItems` and
`onRenderIcon` properties of [ParticipantItem](./?path=/docs/ui-components-participantitem--participant-item#props)
like in the code below. For simplicity, we are using React `useState` to keep the state of every participant
to decide which menu items and icons to show. By clicking a participant in the rendered example below, you can now mute and unmute.

<Source code={InteractiveParticipantListExampleText} />

<Canvas withSource="none">
  <InteractiveParticipantListExample />
</Canvas>
